"use strict";

function secondLargest(array){
    let min=array[0];
    let secondmin=array[1];
    for(let i=2;i<length;i++ ){
        if(array[i]<min){
            secondmin=min; 
        }
        return secondmin;
    }
    
}